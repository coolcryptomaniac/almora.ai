# Next production milestones

After AI Logic + Firebase Hosting are validated:

- Move the model name to Firebase Remote Config.
- Replace whole-collection AI context snapshots with query-based retrieval by intent and geography.
- Add Hindi/English UI controls and voice input/output.
- Add verified source metadata and freshness timestamps to every public record.
- Seed and verify Almora facilities, roads, transport and government-service sources.
- Add wildlife hotspot aggregation from moderated incidents.
- Add jobs matching based on private profiles without exposing candidate data to the public model context.
- Add abuse/rate controls and progressively enforce App Check.
- Add public outcome metrics: reports received, verified, assigned, resolved, and median resolution time.
