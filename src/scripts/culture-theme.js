import{CULTURES,setCultureTheme,read}from'./theme-runtime.js';
const THEMES=CULTURES.map(([id,label])=>[id,label,`theme-${id}`]);
function applyCultureTheme(name){return setCultureTheme(name)}
function mountCultureTheme(){const host=document.querySelector('.topActions');if(!host||document.querySelector('.cultureThemePicker'))return;const wrap=document.createElement('div');wrap.className='cultureThemePicker';wrap.innerHTML=`<button type="button" id="cultureThemeBtn" title="Cultural visual theme" aria-haspopup="menu" aria-expanded="false">✺</button>`;host.insertBefore(wrap,host.querySelector('.weatherTiny'));const main=wrap.querySelector('#cultureThemeBtn'),menu=document.createElement('div');menu.className='cultureMenu';menu.setAttribute('role','menu');menu.innerHTML=CULTURES.map(([id,label])=>`<button type="button" role="menuitem" data-culture="${id}" aria-label="${label} cultural theme"><span class="cultureSwatch"></span><span>${label}</span></button>`).join('');document.body.appendChild(menu);const labelOf=v=>CULTURES.find(x=>x[0]===v)?.[1]||'Aipan';const paint=()=>{main.title=`Cultural theme: ${labelOf(document.documentElement.dataset.cultureTheme)}`};const close=()=>{menu.classList.remove('open');main.setAttribute('aria-expanded','false')};paint();main.onclick=e=>{e.stopPropagation();const opening=!menu.classList.contains('open');close();if(opening){menu.classList.add('open');main.setAttribute('aria-expanded','true')}};menu.querySelectorAll('[data-culture]').forEach(b=>b.onclick=e=>{e.stopPropagation();applyCultureTheme(b.dataset.culture);paint();close()});menu.onclick=e=>e.stopPropagation();document.addEventListener('click',close);addEventListener('resize',close,{passive:true})}
function initCultureTheme(){read();mountCultureTheme()}
export{initCultureTheme,applyCultureTheme,THEMES};
