import { auth, db } from './firebase-platform.js';
import { onAuthStateChanged } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js';
import { collection,onSnapshot,query,where,addDoc,serverTimestamp } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js';
const list=document.querySelector('#businesses'),status=document.querySelector('#businessStatus');let user=null;onAuthStateChanged(auth,u=>user=u);onSnapshot(query(collection(db,'businesses'),where('status','==','approved')),s=>{list.innerHTML='';if(!s.size){list.innerHTML='<article><h3>No verified businesses yet</h3><p>Approved local businesses and services will appear here.</p></article>';return}s.forEach(d=>{const b=d.data(),a=document.createElement('article');a.innerHTML=`<h3>${esc(b.name||'Local business')}</h3><p><b>${esc(b.category||'')}</b><br>${esc(b.location||'')}</p><p>${esc(b.description||'')}</p><b>${esc(b.contact||'')}</b>`;list.appendChild(a)})});document.querySelector('#submitBusiness').onclick=async()=>{if(!user){status.textContent='Sign in from Account before submitting a business.';return}const data={ownerUid:user.uid,name:v('businessName',160),category:v('category',120),location:v('businessLocation',160),contact:v('contact',200),description:v('description',2000),status:'pending',createdAt:serverTimestamp()};if(!data.name||!data.category||!data.location){status.textContent='Name, category and location are required.';return}await addDoc(collection(db,'businessSubmissions'),data);status.textContent='Business submitted for moderation.'};function v(id,max){return document.querySelector('#'+id).value.trim().slice(0,max)}function esc(v){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
